## 🔧 Install

```bash
# Optional: create virtual environment
python -m venv venv
source venv/bin/activate

pip install pytest

cd transforms

pip install ".[all]"

cd ../data-process-lib
pip install . 
```
